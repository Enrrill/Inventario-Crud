---
paths:
  - 'resources/js/components/inventory/**'
---

# Inventory

## Componentes inventory: function declarations, data-slot, cn()
Los componentes en resources/js/components/inventory/ siguen: function declarations (no arrow), React.ComponentProps<>, cn() para clases, Tailwind con dark: variant, data-slot attribute. Importar cn desde @/lib/utils. Usar Badge de shadcn/ui como base para badges con cva para variants.
